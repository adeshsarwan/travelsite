window.renderActualPlaces=function(area,host){const list=window.GW_PLACES&&GW_PLACES[area],meta=window.GW_AREA_META&&GW_AREA_META[area];if(!list||!meta||!host)return;const old=host.querySelector('#actual-places');if(old)old.remove();const section=document.createElement('section');section.className='local-picks';section.id='actual-places';section.innerHTML=`<div class="eyebrow coral">ACTUAL PLACES · ${meta[1].toUpperCase()}</div><h2>Now go somewhere<br><em>specific.</em></h2><p class="local-lead">Three real places to turn the area guide into an actual day. Open any one for GoWandria’s fourth-level place guide.</p><div class="local-pick-grid">${list.map((p,i)=>`<a class="${i===0?'local-pick-feature':''}" href="spot.html?area=${area}&place=${p[0]}"><div><small>${p[2]}</small><h3>${p[1]}</h3><p>${p[3]}</p><span>${p[4]}</span><b>Open actual place guide →</b></div></a>`).join('')}</div>`;const nearby=host.querySelector('#nearby');if(nearby)host.insertBefore(section,nearby);else host.appendChild(section);};